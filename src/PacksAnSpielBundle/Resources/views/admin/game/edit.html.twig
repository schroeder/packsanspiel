{% extends 'PacksAnSpielBundle:admin:baseadmin-layout.html.twig' %}

{% block page_title %}{% endblock %}
{% block page_subtitle %}{% endblock %}
{% block flash_messages %}{% endblock %}

{% block page_content %}

    {{ froala_display( myContentHtml ) }}


    <div class="row">
        <div class="col-md-6">
            <div class="box box-default">
                <div class="box-header with-border">
                    <h3 class="box-title">Spiel bearbeiten</h3>

                    <div class="box-body">

                        {{ form_start(edit_form) }}
                        {{ form_widget(edit_form) }}
                        <input type="submit" value="Speichern" class="btn-success btn"/>
                        {{ form_end(edit_form) }}
                    </div>
                    <div class="box-footer">
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="box box-danger">
                <div class="box-header with-border">
                    <h3 class="box-title"></h3>

                    <div class="box-body">
                        <div class="form-group">
                            <a href="{{ path('game_index') }}" class="btn-primary btn">Zur&uuml;ck zur Liste</a>
                        </div>
                        <div class="form-group">
                            {{ form_start(delete_form) }}
                            <input type="submit" value="Löschen" class="btn-danger btn">
                            {{ form_end(delete_form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
